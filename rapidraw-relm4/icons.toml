# Icons bundled into the binary by relm4-icons (build-time gresource). Names are
# the file stem without the `-symbolic.svg` suffix. Used for the right-rail tabs
# and the inpaint "create" cards (and other buttons that read clearer with an
# icon). Keep this list tight — only what's actually referenced.
icons = [
    # Right-rail tabs
    "options-regular",        # Adjust
    "crop-regular",           # Crop
    "layer-diagonal-regular", # Masks
    "paint-brush-regular",    # Inpaint / Brush
    "info-regular",           # Info
    # Inpaint create-grid cards
    "eraser",                 # Quick Erase
    "sparkle-regular",        # Subject
    "person-regular",         # Foreground
    "line-horizontal-4-regular", # Linear
    "circle-regular",         # Radial
    # General UI
    "add-regular",            # Add mask / region menus
    "delete-regular",         # Delete
    "arrow-counterclockwise-regular", # Reset
    "eye-regular",            # Visible toggle
    "eye-off-regular",        # Hidden toggle
]
